use std::collections::btree_map::Range;

use custom_db_wrapper::*;
use fake::faker::internet::raw::FreeEmail;
use serde_json::*;
use fake::{ faker::name::raw::*, Fake, locales::EN, Faker };
use fake::faker::internet;
use std::io::Read;
use std::io::Write;
use fakeit::*;

fn main() {
    
}
